
<%= form_for [@taggable, @tag] do |f| %>

  <td>
	<%= f.collection_select :tag_group_id, TagGroup.all, :id, :name %>

  </td>

  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>

  </div>
<% end %>
